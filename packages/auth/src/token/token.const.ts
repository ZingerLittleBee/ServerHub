const kUserAccessSecret = 'JWT_USER_ACCESS_SECRET'

const kClientAccessSecret = 'JWT_CLIENT_ACCESS_SECRET'

const kClientAccessExpireTime = 'JWT_CLIENT_ACCESS_EXPIRATION_TIME'

export { kUserAccessSecret, kClientAccessSecret, kClientAccessExpireTime }
