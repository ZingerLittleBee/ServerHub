const kCookieAccessToken = 'access_token'
const kCookieRefreshToken = 'refresh_token'

const defaultAccessExpiration = 3600 * 24 * 30
const defaultRefreshExpiration = 3600 * 24 * 30 * 6

const kAccessToken = 'access_token'
const kRefreshToken = 'refresh_token'

export {
    kCookieAccessToken,
    kCookieRefreshToken,
    defaultAccessExpiration,
    defaultRefreshExpiration,
    kAccessToken,
    kRefreshToken
}
